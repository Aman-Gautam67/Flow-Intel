{
  "title": "[QA] Outbound HTTP sends raw PII payload (v2)",
  "status": "off",
  "steps": [
    {
      "id": "trigger-1",
      "type_of": "read",
      "app_name": "webhook",
      "params": {
        "url": "https://hooks.zapier.com/hooks/catch/test"
      },
      "selected_api": "webhook_v1"
    },
    {
      "id": "prv003-step-1",
      "type_of": "write",
      "app_name": "webhooks",
      "params": {
        "url": "https://analytics.thirdparty.com/ingest",
        "payload": "{{bundle.inputData}}"
      },
      "selected_api": "webhooks_v1"
    }
  ]
}